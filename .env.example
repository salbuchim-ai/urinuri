# Keep this file as a template. Put the real key in .env.local.
OPENAI_API_KEY=your_openai_api_key_here

# Optional: override the default model if your API project uses another model.
OPENAI_MODEL=gpt-4o-mini

# Submission default: demo avoids API calls. Set to live only when API billing is ready.
NEXT_PUBLIC_STORY_GENERATION_MODE=demo
